# 以太默认使用本机 Sub2Api，只让项目运行时访问本地模型网关。
# 界面展示的是服务端实际返回的模型名；如果网关把 gpt-5.5 映射到其他模型，会显示实际模型。
# 不使用 Sub2Api 时，把 BASE_URL / MODEL / WIRE / API_KEY 改成任意 OpenAI-compatible 服务即可。
# 真实 key 请放在 .env.local、.env 或运行时环境变量中，不要写进源码。
AETHER_LLM_BASE_URL=http://127.0.0.1:8080/v1
AETHER_LLM_WIRE=responses
AETHER_LLM_MODEL=gpt-5.5
AETHER_LLM_FAST_VISION_MODEL=gpt-5.5
AETHER_LLM_TIMEOUT_MS=180000
# AETHER_LLM_API_KEY=你的本地 Sub2Api API Key

# 兼容旧变量名：AETHER_MODEL_* 和 OPENAI_* 仍可被读取，但不再推荐。
# 不要把 Codex 自己的 auth.json 或 config.toml 作为以太项目配置。

# 联网攻略检索，可选。未配置时只使用本地知识与模型能力。
# TAVILY_API_KEY=你的 Tavily API Key
